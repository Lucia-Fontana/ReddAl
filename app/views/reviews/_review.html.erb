<%# <div class="review-rating" data-score=<%=review.rating %>

<p><%= review.rating %></p>
<p><%= review.content %></p>

<% if user_signed_in? %>

  <% if review.user_id == current_user.id %>

    <%= link_to "Modifica", edit_product_review_path(review.product, review) %>
    <%= link_to "Elimina", product_review_path(review.product, review), data: {turbo_method: :delete} %>

  <% end %>

<% end %>
